### Helm addons (skeleton)

- aws-load-balancer-controller
- external-dns
- cert-manager
- cluster-autoscaler
- fluent-bit (logging)
- metrics-server
- velero

Mỗi addon sẽ có `values.yaml` mặc định và hướng dẫn cài đặt bằng Helm hoặc GitOps.


